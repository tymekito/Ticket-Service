export default class AddBookModel {
  constructor(name, category, author, userId, expirationDate) {
    (this.name = name), 
    (this.category = category);
    (this.author = author);
    (this.userId = userId);
    (this.expirationDate = expirationDate);
  }
}
